node_modules
dist
dist-electron
release
# build/ — 아이콘·entitlements 등 electron-builder 리소스 (release가 패키징 출력)

# IDE
.idea

# 로컬/비공개 문서
docs/
AGENT-CONTEXT.md

# env. 로 시작하는 파일 (예: env.local, env.signing)
env.*

# 기타 로컬·시크릿
*.local
.env
.env.signing
# .env.signing 과 별개 — 예시 파일도 추적 제외
.env.signing.example
